import React from 'react'

const Header = () => {
    return (
        <header className='site-header'>
            <div className="container">
                Header
            </div>
        </header>
    );
}

export default Header